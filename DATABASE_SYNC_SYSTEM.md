# 🔄 Система синхронизации базы данных

## Проблема
- У тебя в `schema.sql` старые политики
- Я создаю новые миграции которые конфликтуют
- Нет единого источника правды
- Каждый раз приходится применять новые политики

## ✅ Решение: Единая финальная миграция

### Файл: `supabase/migrations/20250123_FINAL_fix_all_permissions.sql`

Этот файл - **ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ** для всех политик RLS.

## 📋 Инструкция по применению

### Шаг 1: Открыть Supabase SQL Editor
1. Перейти в Supabase Dashboard
2. Открыть **SQL Editor**

### Шаг 2: Применить финальную миграцию
1. Открыть файл `supabase/migrations/20250123_FINAL_fix_all_permissions.sql`
2. Скопировать **ВЕСЬ** SQL код
3. Вставить в SQL Editor
4. Нажать **Run**
5. Убедиться что нет ошибок

### Шаг 3: Обновить schema.sql (опционально)
После применения миграции можно обновить `schema.sql` для документации:

1. В Supabase Dashboard → SQL Editor выполнить:
```sql
-- Получить все политики для students
SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual, with_check
FROM pg_policies
WHERE tablename IN ('students', 'queue', 'machine_state', 'history')
ORDER BY tablename, policyname;
```

2. Скопировать результат и сохранить в `schema.sql` для документации

## 🎯 Что делает финальная миграция

### 1. Удаляет ВСЕ старые политики
- Все политики для `students`
- Все политики для `queue`
- Все политики для `machine_state`
- Все политики для `history`

### 2. Создает правильные функции
- `is_super_admin()` - проверяет только `is_super_admin = TRUE AND is_banned = FALSE`
- `is_admin()` - проверяет `(is_admin = TRUE OR is_super_admin = TRUE) AND is_banned = FALSE`

### 3. Создает правильные политики

#### Для таблицы `students`:
- **SELECT**: все могут читать (для регистрации)
- **UPDATE own**: пользователь может обновлять свою запись (аватар)
- **UPDATE super_admin**: суперадмин может обновлять всех
- **UPDATE admin**: обычный админ может обновлять всех КРОМЕ суперадминов
- **DELETE super_admin**: суперадмин может удалять всех (кроме себя)
- **DELETE admin**: обычный админ может удалять всех КРОМЕ суперадминов

#### Для таблицы `queue`:
- **SELECT**: все могут читать
- **INSERT own**: пользователь может добавлять себя
- **INSERT super_admin**: суперадмин может добавлять всех
- **INSERT admin**: обычный админ может добавлять всех КРОМЕ суперадминов
- **UPDATE own**: пользователь может обновлять свои записи
- **UPDATE super_admin**: суперадмин может обновлять все записи
- **UPDATE admin**: обычный админ может обновлять все записи КРОМЕ записей суперадминов
- **DELETE own**: пользователь может удалять свои записи
- **DELETE super_admin**: суперадмин может удалять все записи
- **DELETE admin**: обычный админ может удалять все записи КРОМЕ записей суперадминов

#### Для таблицы `machine_state`:
- **SELECT**: все могут читать
- **ALL**: только админы могут управлять

#### Для таблицы `history`:
- **SELECT**: все могут читать
- **INSERT**: аутентифицированные пользователи могут добавлять
- **DELETE**: только суперадмин может удалять

## 📊 Итоговая структура прав

### СУПЕРАДМИН может:
✅ Обновлять ВСЕХ студентов
✅ Удалять ВСЕХ студентов (кроме себя)
✅ Добавлять ВСЕХ в очередь
✅ Обновлять/удалять ВСЕ записи в очереди
✅ Управлять машиной
✅ Удалять историю
✅ Управлять правами других админов

### ОБЫЧНЫЙ АДМИН может:
✅ Обновлять всех студентов КРОМЕ суперадминов
✅ Удалять всех студентов КРОМЕ суперадминов
✅ Добавлять в очередь всех КРОМЕ суперадминов
✅ Обновлять/удалять записи в очереди всех КРОМЕ суперадминов
✅ Управлять машиной
✅ **Редактировать других админов** (это важно!)
✅ **Добавлять других админов в очередь** (это важно!)
❌ НЕ МОЖЕТ управлять суперадминами

### ОБЫЧНЫЙ ПОЛЬЗОВАТЕЛЬ может:
✅ Читать список студентов, очередь, историю, статус машины
✅ Обновлять свою запись (аватар и т.д.)
✅ Добавлять себя в очередь
✅ Обновлять/удалять свои записи в очереди
✅ Добавлять записи в историю
❌ НЕ МОЖЕТ управлять другими пользователями

## 🔄 Система работы с миграциями в будущем

### Правило №1: Никогда не редактировать schema.sql напрямую
`schema.sql` - это только для документации текущего состояния БД.

### Правило №2: Все изменения только через миграции
Когда нужно изменить политики:
1. Создать новый файл миграции в `supabase/migrations/`
2. Формат имени: `YYYYMMDD_description.sql`
3. В миграции:
   - Сначала `DROP POLICY IF EXISTS <имя_политики>`
   - Потом `CREATE POLICY <имя_политики>`

### Правило №3: Документировать изменения
В каждой миграции добавлять комментарии:
```sql
-- ========================================
-- ЧТО ИЗМЕНЯЕТСЯ
-- ========================================
-- Проблема: описание проблемы
-- Решение: описание решения
```

### Правило №4: Тестировать перед коммитом
1. Применить миграцию в Supabase
2. Проверить что все работает
3. Только потом коммитить

## 🛠️ Как я буду работать дальше

### Когда ты просишь изменить права:

1. Я создам новую миграцию в `supabase/migrations/`
2. Покажу тебе что именно изменится
3. Ты применишь миграцию в Supabase SQL Editor
4. Проверишь что все работает
5. Мы закоммитим изменения

### Когда мне нужно узнать текущее состояние БД:

Ты можешь выполнить этот SQL в Supabase SQL Editor и скинуть мне результат:

```sql
-- Получить все политики
SELECT 
  schemaname,
  tablename,
  policyname,
  cmd as operation,
  roles,
  qual as using_clause,
  with_check
FROM pg_policies
WHERE tablename IN ('students', 'queue', 'machine_state', 'history')
ORDER BY tablename, policyname;

-- Получить все функции
SELECT 
  n.nspname as schema,
  p.proname as function_name,
  pg_get_function_arguments(p.oid) as arguments,
  pg_get_functiondef(p.oid) as definition
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public'
  AND p.proname IN ('is_admin', 'is_super_admin')
ORDER BY p.proname;
```

## ⚠️ ВАЖНО: Применить финальную миграцию СЕЙЧАС

**Чтобы синхронизироваться, нужно применить финальную миграцию:**

1. Открыть Supabase SQL Editor
2. Скопировать весь SQL из `supabase/migrations/20250123_FINAL_fix_all_permissions.sql`
3. Выполнить
4. Проверить что нет ошибок

**После этого у нас будет единый источник правды!**

---

**Дата создания:** 2025-01-23
**Статус:** Готово к применению
