# ⚠️ ПРИМЕНИТЬ МИГРАЦИЮ СЕЙЧАС

## Что исправлено

### 1. ✅ Исправлена ошибка SQL в политиках
**Проблема:** `more than one row returned by a subquery used as an expression`

**Решение:** Исправлены подзапросы в политиках - теперь используются правильные алиасы и `NOT EXISTS` вместо подзапросов.

### 2. ✅ Исправлена кнопка "Редактировать" в AdminPanel
**Проблема:** Над другими админами не было кнопки "редактировать"

**Решение:** Исправлено условие - теперь обычный админ видит кнопку редактирования для всех студентов кроме суперадминов (включая других админов).

### 3. ✅ Удалены старые файлы
- `supabase/FIX_PERMISSIONS_SUMMARY.md` (устаревшая документация)
- `supabase/migrations/20250123_restore_admin_permissions.sql` (старая неправильная миграция)

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС

### Шаг 1: Открыть Supabase SQL Editor
1. Перейти в Supabase Dashboard
2. Открыть **SQL Editor**

### Шаг 2: Применить ФИНАЛЬНУЮ миграцию
1. Открыть файл: **`supabase/migrations/20250123_FINAL_fix_all_permissions.sql`**
2. Скопировать **ВЕСЬ** SQL код (от первой до последней строки)
3. Вставить в SQL Editor
4. Нажать **Run**
5. Убедиться что **нет ошибок**

### Шаг 3: Проверить что все работает

#### Тест 1: Войти как обычный админ
1. Войти в приложение как обычный админ
2. Открыть список студентов
3. Найти другого админа (НЕ суперадмина)
4. **Должна быть кнопка "Редактировать"** ✅
5. Нажать "Редактировать" и изменить имя/комнату
6. **Должно сохраниться без ошибок** ✅

#### Тест 2: Проверить что суперадминов нельзя редактировать
1. Найти суперадмина в списке
2. **НЕ должно быть кнопки "Редактировать"** ✅

#### Тест 3: Добавить админа в очередь
1. Открыть админ-панель
2. Выбрать другого админа (НЕ суперадмина)
3. Нажать "Поставить в очередь"
4. **Должно добавиться в очередь** ✅

## 📊 Итоговая структура прав

### СУПЕРАДМИН может:
✅ Редактировать ВСЕХ (кроме других суперадминов)
✅ Добавлять ВСЕХ в очередь (включая админов)
✅ Удалять ВСЕХ (кроме себя и других суперадминов)

### ОБЫЧНЫЙ АДМИН может:
✅ Редактировать всех КРОМЕ суперадминов (включая других админов) 
✅ Добавлять в очередь всех КРОМЕ суперадминов (включая других админов)
✅ Удалять всех КРОМЕ суперадминов

### ОБЫЧНЫЙ ПОЛЬЗОВАТЕЛЬ может:
✅ Редактировать только себя (аватар, имя и т.д.)
✅ Добавлять себя в очередь
❌ НЕ может редактировать других

## 🔍 Если остались проблемы

Если после применения миграции остаются проблемы, выполни эти SQL запросы и пришли мне результаты:

### 1. Получить все политики для students
```sql
SELECT 
  policyname,
  cmd as operation,
  qual as using_clause,
  with_check
FROM pg_policies
WHERE tablename = 'students'
ORDER BY policyname;
```

### 2. Получить все политики для queue
```sql
SELECT 
  policyname,
  cmd as operation,
  qual as using_clause,
  with_check
FROM pg_policies
WHERE tablename = 'queue'
ORDER BY policyname;
```

### 3. Проверить функции is_admin() и is_super_admin()
```sql
SELECT 
  p.proname as function_name,
  pg_get_functiondef(p.oid) as definition
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public'
  AND p.proname IN ('is_admin', 'is_super_admin')
ORDER BY p.proname;
```

---

**Дата:** 2025-01-23
**Статус:** Готово к применению
**Приоритет:** ВЫСОКИЙ - применить как можно скорее

**После применения этой миграции база будет полностью синхронизирована!** 🎉
