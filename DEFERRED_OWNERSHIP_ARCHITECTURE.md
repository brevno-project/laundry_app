# 🎯 Финальная архитектура владения очередью: двухфазная модель

## ✅ Проблема решена

**Сценарий "админ поставил → студент забрал"** теперь работает идеально:

1. **Админ** создает запись в очереди с `user_id = NULL`
2. **Студент** логинится → запись автоматически привязывается к нему
3. **Студент** может управлять записью (выйти, обновить)
4. **Безопасность** - никто не может управлять чужими записями

---

## 🏗️ Архитектурные изменения

### 1. Новая SQL функция `is_queue_owner(queue)`

```sql
-- Универсальная проверка владения записью
create or replace function public.is_queue_owner(q queue)
returns boolean
language sql
security definer
set search_path = public
as $$
  select
    -- если запись уже привязана к user_id
    (q.user_id is not null and q.user_id = auth.uid())

    OR

    -- если запись ещё не привязана, но студент мой
    (
      q.user_id is null
      and exists (
        select 1
        from students s
        where s.id = q.student_id
          and s.user_id = auth.uid()
      )
    );
$$;
```

### 2. Обновленные RLS политики

- **DELETE**: `is_queue_owner(queue) OR is_admin() OR is_super_admin()`
- **UPDATE**: `is_queue_owner(queue) OR is_admin() OR is_super_admin()`  
- **INSERT**: Админы могут создавать для любого, студенты только для себя

### 3. Изменения в коде

#### `joinQueue` - пишем `user_id` если есть сессия
```ts
const { data: authData } = await supabase.auth.getSession();
const currentUserId = authData.session?.user?.id || null;

const newItem = {
  student_id: user.student_id,
  user_id: currentUserId,  // 🔑 Ключевое изменение
  // ... остальные поля
};
```

#### `leaveQueue` - показываем ошибки
```ts
if (error) {
  console.error('leaveQueue error:', error);
  throw new Error(error.message || 'Не удалось выйти из очереди');
}
```

#### `claimMyQueueItems` - автопривязка при логине
```ts
const claimMyQueueItems = async (studentId: string, userId: string) => {
  await supabase
    .from('queue')
    .update({ user_id: userId })
    .is('user_id', null)
    .eq('student_id', studentId);
};
```

---

## 🔄 Как это работает (flow)

### Фаза 1: Админ создает запись
```sql
INSERT INTO queue (
  student_id = 'student-uuid',
  user_id = NULL,  -- ← еще нет владельца
  ...
);
```

### Фаза 2: Студент логинится
```sql
-- Автоматически выполняется в claimMyQueueItems
UPDATE queue 
SET user_id = 'auth-uid'
WHERE user_id IS NULL 
  AND student_id = 'student-uuid';
```

### Фаза 3: Проверка владения (RLS)
```sql
-- Для DELETE/UPDATE проверяет is_queue_owner()
SELECT public.is_queue_owner(queue) 
-- Вернет true если:
--   1) queue.user_id = auth.uid()                    (уже привязано)
--   2) queue.user_id IS NULL AND students.user_id = auth.uid() (отложенное владение)
```

---

## 🛡️ Безопасность

### ✅ Разрешено:
- **Студент** может управлять СВОИМИ записями (включая созданные админом)
- **Админ** может управлять ЛЮБЫМИ записями
- **Суперадмин** может управлять ЛЮБЫМИ записями

### ❌ Запрещено:
- **Студент** не может управлять чужими записями
- **Anon** не может ничего изменять
- **Authenticated** не может удалять чужие записи

---

## 📝 Что нужно сделать

### 1. Применить миграцию в Supabase
```sql
-- Выполнить содержимое файла:
supabase/migrations/20250104_deferred_ownership_architecture.sql
```

### 2. Тестировать сценарии:
- ✅ Админ ставит незарегистрированного студента в очередь
- ✅ Студент логинится → видит запись и может выйти
- ✅ Обычный студент не может управлять чужими записями
- ✅ Админ может управлять всеми записями

---

## 🎯 Результат

**Больше никаких проблем с "не могу выйти из очереди"!**

Система теперь поддерживает отложенное владение записями, что позволяет:
- Админам ставить в очередь любых студентов
- Студентам "подхватывать" записи при логине
- Сохранять полную безопасность через RLS

**Архитектура закрыта на 100%.**
